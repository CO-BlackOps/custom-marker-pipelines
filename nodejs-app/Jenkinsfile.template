pipeline {
  agent { label 'nodejs-app' }
    options { 
      buildDiscarder(logRotator(numToKeepStr: '4'))
    }
      stage('Test') {
      steps {
        container('nodejs') {
          echo 'Hello World!'   
          sh 'java -version'
        }
      }
    }
}
